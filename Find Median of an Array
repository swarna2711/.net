using System;
using System.Linq;
class Program {
    static void Main() {
        int[] arr = { 7, 3, 1, 9, 5 };
        Array.Sort(arr);
        int mid = arr.Length / 2;
        Console.WriteLine("Median: " + (arr.Length % 2 == 0 ? (arr[mid - 1] + arr[mid]) / 2.0 : arr[mid]));
    }
}
