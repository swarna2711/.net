using System;
class Program {
    static void Main() {
        Console.Write("Enter a string: ");
        string str = Console.ReadLine(), rev = new string(str.Reverse().ToArray());
        Console.WriteLine(str == rev ? "Palindrome" : "Not Palindrome");
    }
}
